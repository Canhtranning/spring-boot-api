[phases.setup]
nixPkgs = ["jdk17_headless", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -Dspring.profiles.active=prod -Dserver.port=$PORT -Xmx300m -jar target/spring-boot-api-1.0.0.jar"
